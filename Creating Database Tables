For now, this is a text file to help organize the creation of the 	
tables.

This creates a database file where all the tables would be stored.
sqlite3 fireeye_data.db

This creates the base table, will need to be modified for additional
data when added.

CREATE TABLE base_data_template (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
	temperature REAL,
	humidity REAL,
	moisture_level REAL
);


CREATE Flights (
	FlightID INT AUTO_INCREMENT PK
	Flight
	Date
	StartTime
	EndTime -- Can be NULL till completion
	Duration -- Can be NULL till completion
	FlightDataLink
)

CREATE FlightData (
	DataID
	FlightID
	Timestamp -- Every 10(?) seconds
	Latitude
	Longitude
	Altitude
	Temperature
	Humidity
	ImagePath -- Labled with appropriate time
)
